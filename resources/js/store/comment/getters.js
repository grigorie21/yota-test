const getters = {
    model: state => {
        return state.model;
    },
    node: state => {
        return state.node;
    },
    modelTree: state => {
        return state.modelTree;
    },
    loading: state => {
        return state.loading;
    },
};

export default getters;
